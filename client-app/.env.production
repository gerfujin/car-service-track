# Production API base URL - set to the public URL where the ASP.NET backend is deployed
# Example: VITE_API_BASE_URL=https://api.yourdomain.com
# When using Docker Compose with nginx proxy (see nginx.conf), leave empty so requests go to same origin
VITE_API_BASE_URL=https://alejeg-pp.proxy.itcollege.ee